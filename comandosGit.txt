git config --global user.name
git config --global user.name

git add .
git commit -m "mensaje" *// subir las modificaciones 
git push

git fetch
git pull    *//  descargar 

git reset --hard  *// volver a recuperar 
git clone  https//git....  *// para entrar de otro repositorio 
